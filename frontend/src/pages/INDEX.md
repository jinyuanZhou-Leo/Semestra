<!-- ⚠️ Once this folder changes, update me. -->

Route-level page components bound by React Router.
Includes auth screens, a separate marketing landing page, and program/semester/course workspaces with shared semantic empty-state treatment.
Each page composes shared contexts, hooks, components, and service APIs.

| File | Role | Description |
|------|------|-------------|
| CourseHomepage.tsx | Route page | Course workspace route with compact workspace navigation, query-cache-backed parent Program/Semester breadcrumb reuse, a single-line `Semester / Course` title treatment with semester-sibling course switching from the course-name dropdown, current-tab preservation across sibling-course jumps, sibling-course detail prefetch before navigation so quick switches avoid a full homepage skeleton reload, a compact `kbd` hint for `cmd/ctrl + ↑↓`, throttled sibling-course switching to absorb held keys, directional course-name motion on successful switches, boundary shake feedback when no further course exists, Program-derived default course colors in settings, LMS course link/sync/unlink actions plus targeted LMS/Gradebook/Calendar cache invalidation after link changes, gradebook-owned course stats, runtime-governed plugin inheritance from the parent Semester for assigned Courses plus a dedicated unassigned-Course plugin management panel backed by Course runtime payloads, host-reserved Dashboard/Settings shell tabs that no longer depend on backend tab rows, slash-command actions for switching any current-course tab with resolved tab display names and icons, jumping to sibling semester courses, or opening the widget picker, item-level unavailable-tab empty states, registered plugin settings sections with shared manifest-icon plugin headers plus explicit scope ids, no local add-tab lifecycle, widget availability filtered by enabled plugins including already-persisted dashboard widgets, visible-tab settings runtime preloading, terminal missing-plugin settings fallbacks, unavailable-widget force cleanup, course-aware widget settings context passthrough, workspace-scoped plugin-host navigation context wiring, per-tab runtime instance scope for plugin UI state, shared wheel-driven horizontal tab scrolling, tab content fade-in on every active-tab switch, and settings autosave that now flushes through the shared immediate course-commit path instead of hand-rolled refresh cycles. |
| courseHomepageNavigation.test.ts | Test file | Regression coverage for Course Homepage tab restoration so sibling-course jumps keep the current tab type when the destination course exposes the same tab and otherwise fall back deterministically. |
| courseHomepageNavigation.ts | Helper module | Route-local tab-selection helper that resolves same-tab restoration across sibling-course navigation before falling back to the first visible tab. |
| CreateSemesterWizardPage.tsx | Route page | Standalone Program-hosted Semester creation wizard with draft-first persistence, setup-aware Basics/Courses/Plugins/per-plugin Setup/Review flow, a Semester-settings-aligned shadcn date-picker basics step, locked previous/next-only host navigation, animated step-shell transitions, compact animated bottom navigation labels, a destructive first-step exit action back to Program, Program-installed plugin selection rendered from backend plugin identity plus frontend manifest icons, debounced draft autosave plus flush-on-step-change behavior, refetch-safe local draft hydration that no longer overwrites newer unsaved basics/plugin-setup edits, explicit draft discard, draft-create conflict recovery that resumes an existing server draft before retrying updates, optimistic single-toggle cache sync plus transactional bulk-enable-disable cache reconciliation, setup-step visibility derived from the union of enabled Semester activations and plugin-system setup payloads, plugin-system-backed setup steps that validate on Next, support host-rendered DSL fields or plugin-owned custom setup/review UI, pass draft `semesterId/programId` context into custom setup/review components so plugins can use normal APIs directly during setup, merge saved raw `setup_values` into review rendering, invalid-step normalization when enabled plugins no longer contribute setup, finalize-safe draft query teardown that avoids refetching draft-only setup APIs after activation, full-height mobile-safe Courses and Plugins steps with vertical inner scrolling that avoids shared-shell gutter gaps, condensed shadcn pagination with ellipsis when setup-heavy flows create many steps, a concise single-panel review summary with prose-style course/plugin recaps, server-backed review blockers, plugin setup summaries, final draft activation, and build-clean step scaffolding. |
| CreateSemesterWizardPage.test.tsx | Test file | Route-level regression coverage for wizard basics date validation, plugin availability filtering, immediate setup-step appearance after enabling a setup-bearing plugin, draft-create conflict recovery, per-plugin setup-step normalization after plugin toggles or stale draft recovery, plugin-system-backed setup-step visibility when activation payloads lag behind setup payloads, custom-or-DSL plugin setup validation/review rendering, condensed pagination behavior for setup-heavy step counts, compact bottom-navigation labels plus destructive Program exit treatment, create-semester handoff safety after draft activation, invalid-step fallback, stale-refetch no-clobber behavior, and review-blocker primary-action gating inside the standalone Semester creation wizard. |
| HomePage.tsx | Route page | Home workspace route with responsive create-program surface (desktop dialog + mobile drawer), program overview cards, and a standardized create empty state. |
| LandingPage.tsx | Route page | Public marketing page mounted only on `/landing`, separate from the product-first root entry flow. |
| LoginPage.tsx | Route page | Dedicated sign-in route that keeps existing-account intent explicit, offers password and email-code sign-in inside shadcn `Tabs`, adds common-domain autocomplete on editable email fields after `@`, keeps unknown-email and wrong-password responses intentionally indistinguishable, uses the email-driven continuation flow without exposing whether an email exists, hands verified new emails across to the sign-up page with an explicit next-step message instead of silently switching intent, exchanges credentials or verified email-login state or Google identity for a cookie-backed session before bootstrapping the auth context, returns users to the remembered protected route after session-expiry re-login instead of always sending them to `/`, and keeps field-level shadcn validation feedback without layout-shifting error rows while auth-route shell transitions keep the shared artwork panel mounted. |
| ProgramDashboard.tsx | Route page | Program workspace route with compact mobile overview stat strip, a lightweight entry into the standalone Create Semester wizard with draft resume/discard handling, hidden draft Semesters in the dashboard grid, extracted semester delete controls, a right-aligned shadcn-style settings action that navigates to a dedicated Program settings page, slash-command access to Program-level actions such as `Add Course`, Program-level subject-color management, shared course tag colors reused by Course List and Todo, shared one-decimal GPA-percentage display across overview and course rows, All Courses support for unassigned Program-level courses, synchronized assigned/unassigned refresh after mutations, an edit-mode toggle that swaps from pencil to an explicit exit icon while exposing fixed-size per-row destructive actions behind shadcn `AlertDialog` confirmation, tri-state All Courses sorting that cycles original/ascending/descending order, and standardized not-found fallbacks. |
| ProgramSettingsPage.tsx | Route page | Dedicated Program settings route with breadcrumb-aware navigation that preserves a single current-page marker even in not-found fallbacks, shared Program entity context, separated LMS/general sections, Program-level plugin lifecycle management, registered Program plugin settings sections with explicit scope ids, subject-color defaults, and autosave flush on back navigation. |
| RegisterPage.tsx | Route page | Dedicated sign-up route that keeps account-creation intent explicit, starts with email or Google instead of a login form, adds common-domain autocomplete on the email step after `@`, keeps send-code responses non-enumerating, validates mailbox ownership through shadcn `InputOTP`, collects nickname plus password only after verification, hands verified existing-account emails back to the sign-in page with an explicit sign-in message instead of silently logging in, exchanges the completed registration or Google identity for a cookie-backed session, bootstraps auth state, validates email format client-side, and keeps field-level shadcn invalid states without layout-shifting error rows while auth-route shell transitions keep the shared artwork panel stable. |
| ResetPasswordPage.tsx | Route page | Password-reset route that now adds common-domain autocomplete on the email step after `@`, sends a Resend-backed email verification code, validates ownership with shadcn `InputOTP`, applies a new password through the backend reset-complete flow, and restores the cookie-backed session on success. |
| SemesterHomepage.tsx | Route page | Semester workspace route with compact workspace navigation, query-cache-backed parent Program breadcrumb reuse, dashboard-only overview stats with shared one-decimal GPA-percentage display, runtime-governed plugin availability from Program/Semester payloads, host-reserved Dashboard/Settings shell tabs that no longer depend on backend tab rows, slash-command actions for switching any current-semester tab with resolved tab display names and icons, jumping into semester courses, or opening the widget picker, item-level unavailable-tab empty states, a dedicated Semester plugin-management settings card that now lists all Program-enabled plugins with off-state rows for not-yet-imported Semester plugins while exposing only toggle/info actions, a host-owned semester course-management table in Settings, registered plugin settings sections with shared manifest-icon plugin headers plus explicit scope ids, no local add-tab lifecycle, widget availability filtered by enabled plugins including already-persisted dashboard widgets, visible-tab settings runtime preloading, terminal missing-plugin settings fallbacks, unavailable-widget force cleanup, semester-aware widget settings context passthrough, workspace-scoped plugin-host navigation context wiring, per-tab runtime instance scope for plugin UI state, shared wheel-driven horizontal tab scrolling, tab fade transitions, semester settings including Reading Week persistence through the shared immediate semester-commit path, and standardized unavailable/not-found empty states. |
| semesterHomepageNavigation.test.ts | Test file | Regression coverage for Semester Homepage tab restoration so reorder-driven tab-id changes preserve the current tab type before falling back deterministically. |
| semesterHomepageNavigation.ts | Helper module | Route-local tab-selection helper that resolves semester active-tab restoration across reorder-driven tab-id changes before falling back to the first visible tab. |
| SettingsPage.tsx | Route page | Settings workspace route with debounced auto-save feedback for global defaults, a dedicated multi-integration LMS management section with provider cards, saved-status feedback, validate-gated persistence, shadcn `Field`-based standard form layout, right-aligned switch controls, background plugin idle-preload preference persistence, responsive appearance controls, backup restore drag-drop dialog flow, overflow-safe account/version text on mobile, account sign-out wiring, and an irreversible delete-account danger zone that keeps the destructive explanation inside a confirmation dialog where the user must type the exact sentence `我确认删除账号` before clearing the local auth session and routing back to login. |
