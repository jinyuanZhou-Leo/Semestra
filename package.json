{
  "name": "semestra",
  "private": true,
  "version": "2.2.10",
  "scripts": {
    "build": "npm --prefix frontend run build",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "sh -c 'cd backend && uv run uvicorn main:app --reload --host 0.0.0.0 --port 8000'"
  },
  "dependencies": {
    "ccusage": "^18.0.11"
  }
}
